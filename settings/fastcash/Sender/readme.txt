The index.php implements all the sender components to start a transaction

Its a ready to use file. 

You just need to fill in the default-data of a transaction, with your clients data and a, following the comments instructions, presents at the file.

We recommend using your internal orderID as the TID ( Transaction Identifier ).